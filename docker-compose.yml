services:
  zensical:
    image: zensical/zensical:latest
    container_name: zensical-docs
    user: "1000:1000"
    ports:
      - "8000:8000"
    volumes:
      - .:/docs
    working_dir: /docs
    command: serve -f mkdocs.toml --dev-addr 0.0.0.0:8000
